# Server Environment Variables

# Required (Supabase)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Required in production
JWT_SECRET=replace_with_a_long_random_secret

# Server
PORT=3001
NODE_ENV=development

# CORS
# Primary frontend origin (exact match)
FRONTEND_URL=http://localhost:5173
# Optional additional allowed origins (comma-separated)
# CORS_ALLOWED_ORIGINS=https://solve2win.com,https://www.solve2win.com
# Set to 1 in production if you want to deny requests with no Origin header
# STRICT_CORS=1

# Reverse proxy (set to 1 when behind a proxy like NGINX/Render)
# TRUST_PROXY=1

# Rate limits (per 15 minutes)
# RATE_LIMIT_MAX=600
# AUTH_RATE_LIMIT_MAX=60

# Request body limits
# JSON_BODY_LIMIT=100kb
# URLENCODED_BODY_LIMIT=100kb

# Optional admin debug logging (do not enable in production)
# ADMIN_DEBUG_LOGS=1

# Duplicate / multi-account prevention
# Set to 0 to disable all duplicate checks (not recommended)
# DUPLICATE_PREVENTION=1
# Auto-ban new registrations that match duplicate rules
# AUTO_BAN_DUPLICATES=0
# Similarity threshold for normalized names (0..1)
# DUPLICATE_NAME_SIMILARITY=0.88
# How many accounts can be created per device (default: 1)
# MAX_ACCOUNTS_PER_DEVICE=1
# How many accounts can be created per IP in the time window (default: 3)
# MAX_ACCOUNTS_PER_IP=3
# IP_WINDOW_HOURS=24
